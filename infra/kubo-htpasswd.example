# HTTP basic auth credentials for the KUBO API gateway (nginx).
# Copy this file to /var/www/ipfs/kubo-htpasswd (NOT tracked in git).
# Generate with: htpasswd -c /var/www/ipfs/kubo-htpasswd admin
admin:$apr1$REPLACE_WITH_REAL_HASH
